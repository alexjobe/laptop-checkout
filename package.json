{
  "name": "laptop-check-out",
  "version": "1.0.0",
  "description": "An app that tracks laptop usage, including the user it is checked out to, the manager who approved the check out, an ETA of return, the serial number, and whether the laptop was returned.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alex Jobe",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "ejs": "^2.6.2",
    "express": "^4.17.1",
    "method-override": "^3.0.0",
    "mongoose": "^5.6.0"
  },
  "devDependencies": {
    "nodemon": "^1.19.1"
  }
}
